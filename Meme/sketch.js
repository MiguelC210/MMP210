// Meme version one

function setup() {
    createCanvas(500, 500);
}

function draw() {
    background(0);
    fill("yellow");
    textAlign(CENTER, CENTER);
    textSize(100);
    textFont("Comic Sans MS");
    text("MEME", width/2, height/2);
}